auth:
  provider: keycloak
  audience: AUDIENCE
  identifier: IDENTIFIER
  client_auth_secret: CLUSTER-client-auth-secret
  # Change this value ^^^ to match the name of your cluster (e.g. test, dev, etc.)
  domain: https://keycloak.CLUSTER.com
ckg:
  url: https://CLUSTER.com:8443/
